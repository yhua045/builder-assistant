const useColorScheme = () => ({ colorScheme: 'light' });

const verifyInstallation = () => {};

const vars = (value) => value;

const cssInterop = () => {};

module.exports = {
  useColorScheme,
  verifyInstallation,
  vars,
  cssInterop,
};
